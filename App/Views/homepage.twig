{% extends "layout.twig" %}

{% block content %}
    <div class="row">
        {% if message is defined %}
            {{ message|raw }}
        {% else %}
            geen message?
        {% endif %}
    </div>
{% endblock %}

